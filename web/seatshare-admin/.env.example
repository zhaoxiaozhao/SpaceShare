# 环境变量配置模板（复制为 .env 使用；.env / .env.prod 已被 gitignore，不入库）
#
# 后台 API 地址。可选：
#   1) 留空（默认）→ 使用相对路径 /admin-api：
#      - docker 生产：由 nginx 反代到 api 容器
#      - 本地 dev：由 vite.config.js 的 proxy 转发到 http://localhost:5000
#   2) 填写完整域名（例如生产后台）→ 直连该后台，实现本地一键切换 prod：
VITE_ADMIN_API_BASE=https://friendlyseat-api-302731-11-1474904162.sh.run.tcloudbase.com
